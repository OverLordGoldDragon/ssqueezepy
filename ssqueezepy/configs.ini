## configs
# IS_PARALLEL
parallel=1

## wavelets
# morlet
mu=13.4
dtype=float32

# bump
mu=5
s=1
om=0
dtype=float32

# cmhat
mu=1
s=1
dtype=float32

# hhhat
mu=5
dtype=float32

## _gmw
# gmw
gamma=3
beta=60
norm=bandpass
order=0
centered_scale=False
dtype=float32

## _stft
# stft
dtype=float32

## utils.cwt_utils
# make_scales
downsample=4

## visuals
# _maybe_title
fontsize=16
weight=bold
loc=left



#### END
#### do not edit above line ####
#### edits below this line have no effect ####

ssqueezepy defaults to values above internally.
`ssqueezepy.wavelets.morlet`, for example, has `mu=None` in
function signature, and if left as such, will use value above.

Edit as `name = value` (same as `name=value` or `name =value` etc)
`'string'` same as `string`.

Currently, adding new `# ` or `## ` entries will have no effect,
i.e. only ones already listed in original `ssqueezepy/configs.ini`
are functional.
